server {
    listen 80;
    server_name elvira.lib;
    location /books {
        root /usr/share/nginx/html;
        try_files $uri =404;
    }
    location /stub_status {
        stub_status;  # Для метрик нагрузки
    }
}